NCL language can not distinguish the variables with same name in GRIB2, so we use pygrib parkage in python to deal this problem.
But we meet another problem, pygrib cannot be installed in anaconda. Then we installed pygrib in the Linux default python.
We use magic line to enable the Linux default python.